/**
 * SecretTalk
 * Admin Module
 * Version: 2.1
 */

const statistics = require("./statistics");
const menu = require("./menu");
const chatRoutes = require("./chat.routes");

module.exports = {
    statistics,
    menu,
    chatRoutes
};
